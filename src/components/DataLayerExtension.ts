import BaseComponent from "./Base";

/**
 * Parent class for all components that extend the data layer.
 */
export default class DataLayerExtension extends BaseComponent {
  public type = "DataLayerExtension";
}